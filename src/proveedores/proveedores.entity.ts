// Define una clase llamada 'Proveedores'
export class Proveedores {
    // Declaración de propiedades de la clase 'Proveedores'

    // Propiedad 'id' de tipo string, que generalmente se utiliza para identificar de manera única a un proveedor
    idprov: string

    // Propiedad 'nombre' de tipo string, que almacena el nombre del proveedor
    nombre_prov: string

    // Propiedad 'correo' de tipo string, que almacena la dirección de correo electrónico del proveedor
    correo: string

    // Propiedad 'telefono' de tipo string, que almacena el número de teléfono del proveedor
    telefono: string

    // Propiedad 'direccion' de tipo string, que almacena la dirección del proveedor
    direccion: string

}

/** 
 * En resumen, esta clase Proveedores define la estructura de datos de un proveedor en tu aplicación.
 * Cada propiedad representa un atributo del proveedor, como su nombre, correo electrónico, etc. 
 * Esta clase se utiliza típicamente como una entidad en un sistema, y se puede utilizar para crear, 
 * leer, actualizar y eliminar registros de proveedores en una base de datos u otras operaciones relacionadas 
 * con los proveedores en la aplicación. 
*/